<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="android:screenOrientation">portrait</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.Settings" parent="AppTheme.NoActionBar">
    </style>
    
    <style name="AppTheme.AlarmInfo" parent="AppTheme.NoActionBar">
        <item name="editTextStyle">@style/AlarmInfoEditText</item>
        <item name="android:textViewStyle">@style/AlarmInfoTextView</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.Settings.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
    </style>

    
    <style name="AlarmInfoEditText" parent="Widget.AppCompat.EditText">
        <item name="android:padding">8dp</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="AlarmInfoTextView">
        <item name="android:padding">8dp</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>
